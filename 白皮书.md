XuePilot 派乐伴学 - 系统架构与生态宪章 (V9.0)

定位: 专为家庭与创新教育机构打造的下一代 AI 伴学系统 (Homeschooling OS)。
核心信仰: AI 时代，孩子不再是干活的工具，而是思想的源头。不教标准答案，训练“提问、判断、整合”的底层心智。
技术底线: 真实映射全球教育体系，绝不使用硬编码打补丁；全系统具备极高容错与降级防御能力。

一、 宏观生态矩阵 (Dual-Domain Architecture)

系统采用“一门双城”的 SaaS 标准架构，物理隔离品牌展示与核心应用。

官方门户网 (Landing Page)

域名: www.xuepilot.com

仓库: xuepilot-landing

职能: 品牌展示、痛点转化、动态展示系统引擎。通过 Hashnode GraphQL API 自动同步博客 (blog.xuepilot.com) 的最新文章。引导用户注册进入核心中枢。

核心应用中枢 (Web App)

域名: app.xuepilot.com

仓库: xuepilot.com.web

职能: 提供所有的学习、交互、家控雷达及系统引擎。所有核心操作基于 Supabase Auth 的权限隔离，必须鉴权登录后才可使用。

二、 App 端文件拓扑图 (Physical File Structure)

当前 app.xuepilot.com 仓库下仅且必须包含以下核心文件，严禁产生冗余多维目录：

/ (Root)
 ├── index.html       # [大门] 纯净登录/注册网关 (Supabase 鉴权与数据拓荒)
 ├── dashboard.html   # [大厅] 统一应用大厅 (Launchpad) & 密码保护的家控中枢
 ├── classroom.html   # [C端] 3D 星球教室 (纯粹的学生知识宇宙，剥离编辑权)
 ├── simulator.html   # [C端] AI 指挥官模拟舱 (科幻沉浸式判官游戏)
 ├── manifest.json    # PWA 配置文件 (用于将网页打包成桌面端/移动端应用)
 ├── sw.js            # PWA 离线缓存引擎 (Service Worker)
 ├── vercel.json      # 跨域请求与大模型 API 路由代理配置
 └── 白皮书.md         # 架构记录真理源宪章 (即本文档)


三、 用户流转与状态机路由 (State & Routing)

1. 登录网关 (index.html)

真实连接: 直接硬编码 Supabase URL 与 Anon Key 建立全局通信，依赖 Auth 进行身份与行级数据（RLS）隔离。

数据拓荒引擎: 首次注册/登录或触发 Debug 时，若检测到本地无缓存星系，将强制注入“太阳系演示星系”与“默认学员”，杜绝白屏卡死。

极速跃迁: 鉴权通过后，不再停留，以毫秒级自动跳转至大厅 dashboard.html。

2. 统一大厅 (dashboard.html)

全系统流量的分发集散地，兼具明暗双层逻辑：

明面区域 (For Students)：陈列 classroom.html、simulator.html，以及内置的启发写作舱和 NOVA 伴读舱。孩子一键进入探索流。

暗面区域 (For Parents)：点击底部的“家长/教师主控后台”，必须通过 4 位数字密码锁 (parentPin) 验证。解锁后展示绝对机密的：多学员档案管理、星系与排课锻造系统、六维学情分析雷达以及大模型 API 配置项。

四、 数据库与云端教研资产引擎 (Data & Curriculum Flywheel)

XuePilot 彻底抛弃纯靠 AI 概率生成的“盲盒模式”，转向**“云端教研数据资产驱动”**的严谨模式。

1. Supabase 多元宇宙架构 (Multiverse Schema)

统一储存于 user_data 表，通过主键 id 实行严格的物理划分：

id: '[Auth UID]'：存储具体家庭的私有学员档案、私人星系坐标、学情雷达等用户数据。

id: 'standard_assets'：全系统共享的公共数据池，存放全球标准化大纲、教案与排课 JSON 树。

2. 云端教研资产飞轮 (Cloud Asset Flywheel)

自动沉淀：当任意指挥官首次请求并成功解析某国、某年级的大纲或排课时，这套极其消耗 Token 的庞大 JSON 资产将被永久上链至 standard_assets。

0.1秒全息秒读：后续任何家庭请求同一标准时，系统直接绕过大模型，从数据库极速拉取并渲染。实现“一次生成，全网永久复用”的算力减负。

3. 血缘降级与 RAG 锚定 (Kinship & Grounding)

血缘降级：无法获取某地区官方大纲时，系统将依文化与教育渊源自动顺延映射（如：缺澳洲则查英国标准）。

绝对防幻觉：支持指挥官在后台为学科手动注入【真实教案原件文本】。一旦开启私有 RAG 投喂，该资产被判定为“特化变体”，绝不上传污染云端公共资产池。

五、 核心交互渲染与 UI 法则 (Geek-Level UX Engine)

1. 灵动 3D 星空渲染 (classroom.html)

粒子网格引擎：引入纯 Canvas 绘制背景。背景深空星点绝对静止，前景星系方可随手势/鼠标拖拽产生偏移。

绝对直线铁律：星核与学科星球的引力连线、游离粒子的牵引线、鼠标的战术扫描射线，必须使用 ctx.lineTo 绘制绝对直线，严禁任何贝塞尔曲线弯曲。

逆向物理缩放补偿：在三维透视场景中，为保证缩略信息标签不失真，采用反向缩放因子 scale(8.333) 进行视觉矫正。

2. 抗 AI 惰性与熔断防御 (Anti-Timeout Mechanisms)

动态课时密度铁律：强制要求大模型为主干学科生成 40-80 节细分课时，综合学科生成 15-40 节。

JSON 极简压缩：在 Prompt 中下达死命令（Fatal Constraint），规定 lessons 数组内仅允许纯字符串格式，单课时名称限 10 字内，彻底杜绝大语言模型因嵌套层级过深导致的输出截断与 JSON 解析错误。

3. 终极沙盒防御体系 (SafeStorage Proxy)

搭载全局 window.safeStorage 虚拟内存代理机制。

彻底无视部分浏览器在“隐身模式/无痕浏览”下强行封禁 localStorage 的拦截行为，确保系统内存变量与缓存状态机的平滑流转。

4. 跨域安全代理 (Reverse Proxy)

绝不将包含大模型厂商 API 的请求直接暴露在客户端网络。

所有的推理请求必须经过 vercel.json 的转发（例如：/proxy/groq 映射到 api.groq.com/v1），保障跨域通信顺畅及系统安全。