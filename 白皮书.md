XuePilot 派乐伴学 - 系统架构与生态宪章 (V10.0)

定位: 专为家庭与创新教育机构打造的下一代 AI 伴学系统 (Homeschooling OS)。
核心信仰: AI 时代，孩子不再是干活的工具，而是思想的源头。不教标准答案，训练“提问、判断、整合”的底层心智。
技术底线: 真实映射全球教育体系，绝不使用硬编码打补丁；全系统具备极高容错与降级防御能力。

一、 宏观生态矩阵 (Dual-Domain Architecture)

系统采用“一门双城”的 SaaS 标准架构，物理隔离品牌展示与核心应用。

官方门户网 (Landing Page)

域名: www.xuepilot.com

仓库: xuepilot-landing

职能: 品牌展示、痛点转化、动态展示系统引擎。通过 Hashnode GraphQL API 自动同步博客 (blog.xuepilot.com) 的最新文章。引导用户注册进入核心中枢。

核心应用中枢 (Web App)

域名: app.xuepilot.com

仓库: xuepilot.com.web

职能: 提供所有的学习、交互、家控雷达及系统引擎。所有核心操作基于 Supabase Auth 的权限隔离，必须鉴权登录后才可使用。

二、 App 端文件拓扑图 (Physical File Structure)

当前 app.xuepilot.com 仓库下仅且必须包含以下核心文件，严禁产生冗余多维目录：

/ (Root)
 ├── index.html       # [大门] 纯净登录/注册网关 (Supabase 鉴权与数据拓荒)
 ├── dashboard.html   # [大厅] 统一应用大厅 (含 3D 星球教室及密码保护的家控中枢)
 ├── simulator.html   # [C端] AI 指挥官模拟舱 (科幻沉浸式判官游戏)
 ├── manifest.json    # PWA 配置文件 (用于将网页打包成桌面端/移动端应用)
 ├── sw.js            # PWA 离线缓存引擎 (Service Worker)
 ├── vercel.json      # 跨域请求与大模型 API 路由代理配置
 └── 白皮书.md         # 架构记录真理源宪章 (即本文档)


三、 用户流转与状态机路由 (State & Routing)

1. 登录网关 (index.html)

真实连接: 直接硬编码 Supabase URL 与 Anon Key 建立全局通信，依赖 Auth 进行身份与行级数据（RLS）隔离。

数据拓荒引擎: 首次注册/登录或触发 Debug 时，若检测到本地无缓存星系，将强制注入“太阳系演示星系”与“默认学员”，杜绝白屏卡死。

极速跃迁: 鉴权通过后，不再停留，以毫秒级自动跳转至应用大厅 dashboard.html。

2. 统一大厅 (dashboard.html)

全系统流量的分发集散地，兼具明暗双层逻辑：

明面区域 (For Students)：陈列 classroom (3D 教室默认视图)、simulator.html，以及内置的【AI 启发写作舱】、【英文智能伴读】与【大语文智能伴读】。孩子一键进入探索流。

暗面区域 (For Parents)：点击底部的“家长/教师主控后台”，必须通过 4 位数字密码锁 (parentPin) 验证。解锁后展示绝对机密的：多学员档案管理、星系与排课锻造系统、六维学情分析雷达以及大模型 API 配置项。

四、 数据库与云端教研资产引擎 (Data & Curriculum Flywheel)

XuePilot 彻底抛弃纯靠 AI 概率生成的“盲盒模式”，转向**“云端教研数据资产驱动”**的严谨模式。

1. Supabase 多元宇宙架构 (Multiverse Schema)

统一储存于 user_data 表，通过主键 id 实行严格的物理划分：

id: '[Auth UID]'：存储具体家庭的私有学员档案、私人星系坐标、学情雷达等用户数据。

id: 'standard_assets'：全系统共享的公共数据池，存放全球标准化大纲、教案与排课 JSON 树。

2. 云端教研资产飞轮 (Cloud Asset Flywheel)

0.1秒全息秒读：当请求某国大纲排课时，系统首先比对云端 standard_assets，实现“一次生成，全网永久复用”的算力减负。

五、 核心交互渲染与 UI 物理法则 (Geek-Level UX Engine)

本章节记录了 XuePilot 引以为傲的前端渲染与物理引擎，在后续迭代中严禁破坏这些数学计算与样式层！

1. 极客级 3D 沉浸式星球引擎 (3D Planet Rendering)

纯 CSS 渲染：系统拒绝加载任何笨重的 3D 库（如 Three.js）。星球的 3D 立体感完全通过 CSS 的极致组合实现。

光影算法：

使用 radial-gradient 制造星球表面的球形反光。

使用 box-shadow: inset -10px -10px 20px rgba(0,0,0,0.8) 制造星球背光面的深邃内发光黑影。

使用外围 box-shadow 制造星球能量辐射的光晕效果。

2. 绝对直线动态丝线网格 (Dynamic Straight-Line Mesh)

这套算法负责构建充满科幻硬核感的深空物理网格。

绝对直线法则 (Never Bend)：所有丝线必须通过 ctx.lineTo 绘制，禁止任何贝塞尔曲线 (bezierCurveTo)。

物理丝线种类：

游离态粒子网 (Wandering Particles)：在背景中自由漂浮，触碰边界反弹。粒子间距离 < 150px 时自动牵引出细微的蓝色直连线。同时受到鼠标微弱的引力拉扯。

星系能量键 (Galactic Energy Bonds)：当激活特定知识星系时，星核与周围的学科星球将被紫色的能量光束硬性连接。

战术扫描探测线 (Tactical Scan Lines)：当鼠标靠近星系能量键（距离 < 800px）时，能量键会高亮变为青色。当鼠标靠近星球（距离 < 250px）时，会射出一条直达星核的青色虚线 (setLineDash) 战术扫描射线。

偏移修正：Canvas 坐标 mouseX 在计算时，严格执行了 - 288px（侧边导航栏宽度）的偏差减除补偿，保证物理牵引完美对齐准心。

六、 防熔断策略与沙盒机制 (Anti-Timeout & Sandbox)

JSON 极简压缩：在 Prompt 中强制规定排课 lessons 数组内仅允许纯字符串格式（限 10 字内），彻底杜绝大语言模型因嵌套过深导致的输出截断。

终极沙盒防御体系 (SafeStorage Proxy)：搭载全局 window.safeStorage 代理机制，全方位对抗浏览器无痕模式强行封禁 localStorage 导致的本地系统崩溃。